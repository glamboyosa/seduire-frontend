import React from 'react';
import { RouteComponentProps } from '@reach/router';
import SingleProduct from '../components/Products/singleProductContainer';
export default (props: RouteComponentProps) => <SingleProduct />;
